spec:
 name: webshot
 services:
 - name: web
   image:
    registry_type: DOCKER_HUB
    registry: botshouse
    repository: webshot
    tag: dev
   http_port: 8000
   health_check:
    http_path: /health